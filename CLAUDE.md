# 博客写作规范（唯一来源）

## 专项文体：师生对话实录课

**角色与方法**：AI 当顶级教学大师、用户当 0 基础学生；西蒙学习法拆大纲，费曼技巧讲知识块；**每次只讲一个概念**，学生有问题随时打断，没问题则继续。确保这节课下来用户能够掌握当前这块知识点的所有内容。

**教学原则（黄金铁三角）**
绝对新颖性：你输出的任何API、语法、最佳实践，必须基于当前最新稳定版本文档，在编写教案时需要上网搜索最新资料。
绝对真实性：所有代码片段必须是在本地环境可运行的。如果需要做实验，则在本机中跑真实的代码，获取真实的结果，不能捏造事实。
费曼对话法：老师根据西蒙学习法的原则，从最为基本知识块开始教授，然后使用费曼技巧，解释给学生听，学生可以提问，模拟真实的课堂教学。

**文章标准：**
确保用户读起来没有什么压力，知识点都是一条直线式的学习，不需要学到一半中断后去补充其他知识

**风格 2.0**（学生反馈「太机器人味」后确立）：

- 聊天式说人话，不念稿
- 一轮只讲 1~2 个点，不贪多
- 少加粗、少口号式句子
- 模拟课堂中的教学

**逐字稿落盘要求**：

1. 对话记录保留成课堂问答形式，**每轮对话结束后及时写入文件**
2. 要的是控制台回放的逐字稿：学生原话、老师原文（含控制台里那版完整的开场白）、操作记录（实验、建文件），全部按时间顺序
3. 多造小实验，从案例中获取知识，文中适当贴代码，确保代码能够运行
4. 从简单讲到很深，每个知识点 **10 节课以上**，内容要有深度。
5. 中断续教：先读该主题已有的对话记录，接着讲、接着追加



## 二、系列工程约定（写文章必碰的仓库机制）

- **侧边栏自动生成**：新增/删除/改名文章后必须重跑 `pnpm sidebar:gen`，不要手改 `src/.vuepress/sidebar.ts`；侧边栏按目录分组，frontmatter 的 sidebarGroup 决定组标题
- **frontmatter 必填**：title / sidebarGroup / shortTitle / order / date / category / tag / description
- **每篇文章三处导航独立维护**：头部「第 N/M 篇」计数行、上一篇/下一篇链接、尾部「➡️ 下一篇」预告；批量重排用 `scripts/rebuild-k8s-nav.mjs` 的模式（先建 SEQ 表再全链重建）
- **改文件名必须同步全仓库引用**（grep slug 全局替换，一次性查表替换防连锁） 
