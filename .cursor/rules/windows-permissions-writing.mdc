---
description: Windows 权限系列文章必须用案例引入的西蒙讲述
globs: src/Windows/permissions/**/*.md
alwaysApply: false
---

# 权限系列写作规范

撰写或加厚 `src/Windows/permissions/` 下章节时：

1. **案例引入**：先用小王（或同类角色）碰到的具体麻烦开场，再推导概念。
2. **西蒙/费曼**：一次只发明一个概念；只用已有站解释；专有名词后置（先人话，再贴学名）。
3. **固定节奏**：场景 → 错误直觉 → 一起推导 → 起学名 → 怎么看见（命令/界面）→ 收束（你现在会了 / 下一站才需要）。
4. **不写**：提权漏洞、绕过 UAC/Deny 的攻击步骤；关 UAC 不作推荐做法。
5. **出处**：关键论断旁标注 Microsoft Learn；站间用相对链接（如 `./10-ace-dacl.md`）。
6. **章末**：保留 `<!-- chapter-nav -->` 导航块；改完同步 `00-index.md` 里该站的一句话简介（若含义变了）。
