# 博客写作规范（唯一来源）

本文件是博客写作规范的**唯一权威源**，收拢自：全局 `~/.claude/CLAUDE.md` 通用规范、`.cursor/rules/` 两份 mdc、历史写作反馈（五件套与反模式）、教学实践约定（三承诺与风格 2.0）。其他位置的旧副本一律以本文件为准，不再单独维护。

适用范围：本仓库 `src/**/*.md` 的撰写与修改。详细演绎（18 条手法 + 范文逐段对照）见 `src/Notes/think/博客写作规范.md`，给人读；本文件给 AI 执行。

---

## 一、通用规范（所有文章必须遵守）

1. **问题驱动**：开篇先回答「这个知识点是为了解决什么问题而存在的」，不要一上来堆定义或术语。
2. **四问结构**：每个核心概念覆盖——是什么（白话本质）/ 为什么（动机、取舍、不用会怎样）/ 怎么做（可操作步骤）/ 背景知识（依赖关系讲清，不留黑洞）。
3. **循序渐进**：从已知到未知，一次只推进一层；先直觉与例子，再抽象与术语；读者能顺着一条线读完，不跳着啃碎片。
4. **先实验再写入**：文中代码、命令、演示必须先在本机实际跑通；真实输出与关键报错写进文章——可适当精简，**不可杜撰**；「命令没有输出」也是结果，照样写；环境差异（OS/版本）影响结果的注明；搭建类内容做成可照抄的操作手册。
5. **采用最新资料**：写前核验官方文档/发行说明是否当前版本；过时 API 与已废弃做法不得当「现行最佳」写，有教学价值的标明「历史/兼容」语境；版本相关结论旁标注出处与大致时间/版本号。
6. **模块循序渐进**：确保当前模块下所有文章知识点逐渐累加、不指向后续文章；若出现指向后文的情况，调整文章顺序，把基本知识放前面。

## 二、写作手法（按文章物理顺序执行）

### 开篇三件套

- **痛点先行**：先「为什么要学」（读者为问题停留，不为答案停留），再给定义
- **路线图一条线**：开篇给出 `① → ② → …` 的课程/章节路线，读者随时知道自己在哪、还剩多少
- **环境与官方入口**：一句话交代实验环境（OS/版本/实验目录）+ 官方文档链接

### 正文推进

- **每节一个概念，一步不跳**：读者掉队发生在作者觉得「显然」的跳步处；新概念的第一个例子必须搭在读者已会的东西上
- **长命令配逐段拆解表**：命令按段拆成「段 | 含义」表格——这是「会用」和「背会」的分界线
- **输出殿后，先说看哪**：先给预期（「注意第 6 列」「三行输出逐个看」），输出只是作证；长输出拆字段或对齐
- **每节一句话总结**：结尾 blockquote 用等式/对仗把本节压成一句能背下来的话
- **比喻一个用到底**：每个比喻只服务一个概念，不混用、不升级、不复用
- **易混点独立成块**：不揉进正文，必须配一个实验性的判别动作
- **插问 / 课堂检查 / 留题**：替读者问出「不好意思问」的问题；上节留题下节回收；用悬念缝合节与节
- **前后篇互链 + 伏笔**：标注概念「从哪来、到哪去」，读者的知识不成孤岛

### 收尾

- **小结串成因果链**：质量标准——只读小结能复原全文逻辑骨架
- **验证步骤 + 收题 + 清理现场**：给读者一条能自己跑的验证命令；开篇/中途的题文末必须收；实验收尾命令 + 实验目录保留可重做
- **参考资料 + 实测注记**：官方文档链接；注明本机实测日期与环境

## 三、反模式（历史反馈「读起来难受」的四种根因，禁止）

| # | 反模式 | 症状 | 药方 |
|---|--------|------|------|
| 1 | 证据链先行 | 先贴大段输出/报错再解释 | 预期在前，输出殿后 |
| 2 | 实验腔 | 「然后我执行了…接着输出显示…」按时间流水账 | 按概念组织，不按时间组织；每节一句话收口 |
| 3 | 200 字长单段 | 「怎么读这行输出」写成一大段 | 拆解表、逐行认读，一段只讲一层 |
| 4 | 回扣滥用 | 每段「如前所述」「下面将会讲到」 | 路线图只给一次；段内自洽，靠结构不靠口头铺垫 |

## 四、专项纪律

- **实机演示禁造假**：必须真跑，不造输出（Windows 权限系列确立，适用于一切平台）
- **占位文档补正文**时：先学习、先实验、再撰写；正文按本文规范写，完成后去掉「占位待学」标记
- 各系列另有自己的结构约定（RabbitMQ、小程序实战篇等）记录在 Claude 记忆中，动哪个系列先看对应记忆

## 五、专项文体：师生对话实录课

**角色与方法**：AI 当顶级教学大师、用户当 0 基础学生；西蒙学习法拆大纲，费曼技巧讲知识块；**每次只讲一个概念**，学生有问题随时打断，没问题则继续。

**逐字稿落盘要求**：

1. 对话记录保留成课堂问答形式，**每轮对话结束后及时写入文件**
2. 要的是控制台回放的逐字稿：学生原话、老师原文（含控制台里那版完整的开场白）、操作记录（实验、建文件），全部按时间顺序、**不加整理**
3. 多造小实验，从案例中获取知识
4. 从简单讲到很深，每个知识点 **10 节课以上**
5. 中断续教：先读该主题已有的对话记录，接着讲、接着追加

**教学三承诺**（对 0 基础学生）：

- 新词先定义——任何术语出现前先给白话定义
- 代码只看照片——不给大段代码让初学者读，关键代码用截图/短段 + 逐行解释
- 学生可随时喊停——听不懂立刻打断，不打断就默认讲懂了

**风格 2.0**（学生反馈「太机器人味」后确立）：

- 聊天式说人话，不念稿
- 一轮只讲 1~2 个点，不贪多
- 少加粗、少口号式句子

## 六、教学路径规划（RoadMap 制作工作流）

**触发**：用户指定一个学习领域 + 目标路径。角色同上（大师 + 0 基础学生，西蒙学习法拆碎）。

**产出 1——总纲 RoadMap 博客**（放目标路径下，命名 `xxx-00-roadmap.md`，frontmatter：title「XX 学习总纲：零基础到资深专家的完整教学大纲」/ order: 0 / shortTitle「00 学习总纲」）：

- 开篇问题驱动：这个领域知识网怎么互锁、乱学会掉什么黑洞
- 领域知识底图（分层表：每层解决什么问题、不懂会怎样）
- 验收标准（分档递进，如「会用/会修/会建/会扩」，锚定认证或毕业设计）
- 西蒙学习法落法：拆碎（0.5~2 天/单元）、单点聚焦、及时反馈（每单元动手实验 + 吃透标准）、连续推进（周期表）
- 学习环境清单；已有文章「弹药盘点」；缺口清单
- 九~十个阶段，每阶段：为什么有这个阶段 + 单元表（单元 | 回答的核心问题 | 动手实验 | 吃透的标准）+ 阶段验收 + 弹药链接
- 资深自检 N 问；总时间线；参考资料（核验时间）
- **版本事实先核验再写**（官方 releases/文档/认证页，标注核验日期）

**产出 2（可选）——占位文档系列**（按总纲在对应目录拆空白占位文档）：

- 每篇占位：frontmatter（description 带【占位待学】）+ 头部导航（第 N/M 篇 + 🚧 占位待学 + 学习大纲回链）+ 五节：要解决的问题 / 知识点清单 / 动手实验（学习时必须真跑）/ 验收标准（checkbox，全部通过才进入下一篇）/ 写作提示（补正文时遵守本文规范）
- 正文待按「先学习、先实验、再撰写」补全，完成一篇去掉占位标记

**旧教材处理**：已有旧系列映射为各阶段「弹药」（学习时回看），或标注「早期系列」；不删除。

**已有先例**：`src/Ai/roadmap/aicon-2026-roadmap.md`、`src/分布式/roadmap/`、`src/云原生/k8s/k8s-00-roadmap.md`、`src/中间件/redis|rocketmq/`、`src/云原生/docker/docker-00-roadmap.md`。

## 七、系列工程约定（写文章必碰的仓库机制）

- **侧边栏自动生成**：新增/删除/改名文章后必须重跑 `pnpm sidebar:gen`，不要手改 `src/.vuepress/sidebar.ts`；侧边栏按目录分组，frontmatter 的 sidebarGroup 决定组标题
- **frontmatter 必填**：title / sidebarGroup / shortTitle / order / date / category / tag / description
- **每篇文章三处导航独立维护**：头部「第 N/M 篇」计数行、上一篇/下一篇链接、尾部「➡️ 下一篇」预告；批量重排用 `scripts/rebuild-k8s-nav.mjs` 的模式（先建 SEQ 表再全链重建）
- **改文件名必须同步全仓库引用**（grep slug 全局替换，一次性查表替换防连锁）

## 八、发布前检查清单（逐项打勾）

- [ ] 开篇说出了「为什么学这个」？
- [ ] 有路线图？读者会不会迷路？
- [ ] 每节只有一个概念？有没有跳步？
- [ ] 长命令有拆解表？
- [ ] 每段输出前先说了看哪？
- [ ] 每节有一句话总结？
- [ ] 易混点独立成块 + 实测？
- [ ] 版本结论标了出处与时间？老做法标了「历史」？
- [ ] 输出全部真实？环境差异注明？
- [ ] 小结能只靠它复原全文骨架？
- [ ] 读者有一条能自己跑的验证命令？
- [ ] （系列文章）`pnpm sidebar:gen` 跑过？三处导航对齐？
