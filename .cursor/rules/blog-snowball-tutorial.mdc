---
description: 滚雪球写作骨架：同一场景/产物逐步加因素；动手实验与概念综述都适用
globs: src/**/*.md
alwaysApply: false
---

# 滚雪球写作（结构专项）

撰写或修改 `src/` 下任意文章时，用这条定**章节骨架**。通用原则见 `blog-writing-standards.mdc`。样板：`src/云原生/docker/docker-13-compose.md`。

**核心**：同一条场景或同一份产物一路长大。每节只加一个因素，当场给出可见效果。概念从现象里长出来，不先背名词。概念综述同样如此：不要开篇百科，用同一故事每次只解开一层。

## 开头（读前就能看见整条路）

1. 痛点场景（读者自己会碰到的麻烦）
2. 根因一句（过程 vs 声明、可写层会丢等），不展开词典
3. 写明「本篇不先背概念」或等价意思
4. **雪球地图表**：列 | 这一球加上去的 | 当场能看见的效果（概念文改成「这一节解开的」|「读完能回答的」）
5. 固定实验目录 / 贯穿全文的那条故事；动手文写环境指纹（OS + 软件版本）+ 官方入口链接

## 每一球 / 每一节

- 标题写清「加了什么」，不要空泛的「深入理解 XXX」
- **动手**：给出**当时完整、可整份复制**的文件；正文只强调相对上一节的增量
- **概念**：仍用开头那条故事；只引入本节必需的一个模型/对比/术语
- 命令或论断之后：真实输出或具体后果 → **解读每一行关键信息代表什么**
- 刚冒出来的名字，立刻钉成小模型（ASCII / 表），不要攒到文末
- 系列里已讲过的，回链，不重讲；第一次出现的词必须有着落
- 坑先记下现象，标明「雪球 N 补」；不要提前讲还用不到的字段
- 可选进阶标 🧗，主线可跳过

## 禁止

```markdown
❌ 开篇「一、XXX 是什么」堆定义，再「二、安装」，再「三、高级特性」
❌ 每节换一套互不相关的例子
❌ 只贴命令和输出，不解释输出
❌ 一节同时引入网络 + 卷 + 健康检查
❌ 概念文开篇抄官方简介，读者仍不知道不用会怎样
```

```markdown
✅ 同一目录/同一故事；地图表；每节一个新因素 + 一个可见效果
✅ 完整可抄 + 叙述只讲增量
✅ 输出里的 Creating / PONG / (nil) 各讲一句
```

## 章末四件套（按需取，动手文尽量齐）

1. **怎么记**：命令或论点对照「在哪一球用过」
2. **历史包袱**：废弃字段、旧命令；用本机警告证明，不当现行最佳
3. **和系列其它篇**：这路上出现过的回链
4. **小结**与雪球一一对应 + **思考题**（指向本篇埋的坑）+ 参考资料（官方 + 本机版本）
